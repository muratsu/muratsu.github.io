# Occupy Matrix

A static blog built with Next.js, Nextra, and `nextra-theme-blog`.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The site exports to `out/` as static files and is ready for Vercel static hosting.
